# 📐 KSIS System Architecture - Visual Guide

## Complete System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                        KSIS ECOSYSTEM                                │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐          ┌──────────────┐          ┌──────────────┐
│  Web Browser │          │ Mobile App   │          │ Mobile App   │
│   (React)    │          │   (iOS)      │          │  (Android)   │
└──────┬───────┘          └──────┬───────┘          └──────┬───────┘
       │                         │                         │
       └─────────────────────────┼─────────────────────────┘
                                 │
                          HTTP + JSON
                                 │
                       ┌─────────▼─────────┐
                       │                   │
                       │   Laravel API     │
                       │  (Port 8000)      │
                       │                   │
                       └─────────┬─────────┘
                                 │
                          Eloquent ORM
                                 │
                       ┌─────────▼─────────┐
                       │                   │
                       │   PostgreSQL      │
                       │   Database        │
                       │                   │
                       └───────────────────┘
```

---

## Backend Architecture (Laravel)

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Laravel Application                              │
│                                                                      │
│  ┌────────────────┐                                                 │
│  │ routes/api.php │ ◄─── Entry Point (All API Routes)              │
│  └────────┬───────┘                                                 │
│           │                                                          │
│           ▼                                                          │
│  ┌─────────────────────────────────────────────────────┐           │
│  │           HTTP Controllers (20 files)                │           │
│  ├─────────────────────────────────────────────────────┤           │
│  │ • AuthController        → Login/Register/Logout     │           │
│  │ • TeacherController     → Teacher CRUD              │           │
│  │ • KpiController         → KPI Management            │           │
│  │ • EvaluationController  → Evaluations               │           │
│  │ • MycpeRecordController → CPE Tracking              │           │
│  │ • NotificationController→ Notifications             │           │
│  │ • ReportController      → Reports & Analytics       │           │
│  │ • ... 13 more controllers                           │           │
│  └──────────┬──────────────────────────────────────────┘           │
│             │                                                        │
│             ▼                                                        │
│  ┌─────────────────────────────────────────────────────┐           │
│  │              Services (Business Logic)               │           │
│  ├─────────────────────────────────────────────────────┤           │
│  │ • ConfigurationService  → Settings Management       │           │
│  │ • ScoringService        → Score Calculation         │           │
│  │ • ReportingService      → Report Generation         │           │
│  │ • EvidenceService       → File Handling             │           │
│  │ • WorkflowService       → Approval Workflows        │           │
│  └──────────┬──────────────────────────────────────────┘           │
│             │                                                        │
│             ▼                                                        │
│  ┌─────────────────────────────────────────────────────┐           │
│  │            Models (Database Access)                  │           │
│  ├─────────────────────────────────────────────────────┤           │
│  │ • User              → users table                    │           │
│  │ • Teacher           → users (role=teacher)           │           │
│  │ • Department        → departments table              │           │
│  │ • Kpi               → kpis table                     │           │
│  │ • Evaluation        → evaluations table              │           │
│  │ • MycpeRecord       → mycpe_records table            │           │
│  │ • Notification      → notifications table            │           │
│  │ • ... 10 more models                                 │           │
│  └──────────┬──────────────────────────────────────────┘           │
│             │                                                        │
│             ▼                                                        │
│  ┌─────────────────────────────────────────────────────┐           │
│  │              PostgreSQL Database                     │           │
│  └─────────────────────────────────────────────────────┘           │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## Request Flow (Example: Teacher Login)

```
Step 1: User Action
┌──────────────┐
│ User enters  │
│ email/pwd    │
│ and clicks   │
│ "Login"      │
└──────┬───────┘
       │
       ▼
Step 2: Frontend (React/Mobile)
┌─────────────────────────────────┐
│ authService.login(email, pwd)   │
│                                 │
│ POST /api/login                 │
│ Body: {                         │
│   email: "teacher@school.edu",  │
│   password: "password123"       │
│ }                               │
└──────┬──────────────────────────┘
       │
       │ HTTP Request
       ▼
Step 3: Laravel Routing
┌─────────────────────────────────┐
│ routes/api.php                  │
│                                 │
│ Route::post('/login',           │
│    [AuthController::class,      │
│     'login'])                   │
└──────┬──────────────────────────┘
       │
       ▼
Step 4: Controller
┌─────────────────────────────────┐
│ AuthController@login            │
│                                 │
│ 1. Validate input               │
│ 2. Check credentials            │
│ 3. Create token                 │
│ 4. Return user + token          │
└──────┬──────────────────────────┘
       │
       ▼
Step 5: Model
┌─────────────────────────────────┐
│ User::where('email', $email)    │
│     ->first()                   │
│                                 │
│ Executes SQL:                   │
│ SELECT * FROM users             │
│ WHERE email = 'teacher@...'     │
└──────┬──────────────────────────┘
       │
       ▼
Step 6: Database
┌─────────────────────────────────┐
│ PostgreSQL                      │
│                                 │
│ Returns user record:            │
│ {                               │
│   id: 1,                        │
│   name: "John Doe",             │
│   email: "teacher@school.edu",  │
│   role: "teacher"               │
│ }                               │
└──────┬──────────────────────────┘
       │
       ▼
Step 7: Response
┌─────────────────────────────────┐
│ JSON Response:                  │
│ {                               │
│   "user": {                     │
│     "id": 1,                    │
│     "name": "John Doe",         │
│     "role": "teacher"           │
│   },                            │
│   "token": "abc123xyz..."       │
│ }                               │
└──────┬──────────────────────────┘
       │
       ▼
Step 8: Frontend Updates
┌─────────────────────────────────┐
│ 1. Store token in storage       │
│ 2. Store user data              │
│ 3. Navigate to dashboard        │
│ 4. Show welcome message         │
└─────────────────────────────────┘
```

---

## Database Schema Relationships

```
┌────────────┐
│   users    │
└─────┬──────┘
      │
      │ 1
      │
      ├──────────┐
      │          │
      │ many     │ many
      ▼          ▼
┌─────────┐  ┌──────────────────┐
│  kpis   │  │ mycpe_records    │
└─────────┘  └──────────────────┘
      │
      │ many
      │
      ▼
┌──────────────────┐
│   evaluations    │
└──────┬───────────┘
       │
       │ 1
       │
       ├───────┬────────────┬────────────┐
       │       │            │            │
       │ many  │ many       │ many       │ many
       ▼       ▼            ▼            ▼
┌────────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│ classroom_ │ │ annual_ │ │feedback │ │reeval_  │
│observation │ │appraisal│ │         │ │requests │
└────────────┘ └─────────┘ └─────────┘ └─────────┘


┌──────────────┐
│ departments  │
└──────┬───────┘
       │ 1
       │
       │ many
       ▼
  ┌────────┐
  │ users  │
  └────────┘
```

---

## MVC Pattern Explained

```
┌─────────────────────────────────────────────────────────┐
│                    MVC Pattern                          │
└─────────────────────────────────────────────────────────┘

Request from User
       │
       ▼
┌──────────────────┐
│   Controller     │ ◄─── Handles HTTP requests
│                  │      Validates input
│  • index()       │      Calls models
│  • store()       │      Returns responses
│  • update()      │
│  • destroy()     │
└────────┬─────────┘
         │
         │ calls
         ▼
┌──────────────────┐
│     Model        │ ◄─── Interacts with database
│                  │      Defines relationships
│  • User          │      Business logic
│  • Teacher       │      Data validation
│  • Kpi           │
└────────┬─────────┘
         │
         │ queries
         ▼
┌──────────────────┐
│   Database       │ ◄─── Stores data
│                  │      Enforces constraints
│  PostgreSQL      │      Indexes
└──────────────────┘
         │
         │ returns data
         ▼
     Controller
         │
         │ formats
         ▼
┌──────────────────┐
│   View (JSON)    │ ◄─── API Response
│                  │      JSON format
│  {               │      Status codes
│    "data": {...} │
│  }               │
└──────────────────┘
         │
         ▼
   Back to User
```

---

## Service Layer Pattern

```
Why Service Layer?

WITHOUT Service Layer:
┌────────────┐
│ Controller │ ◄─── TOO MUCH RESPONSIBILITY!
│            │      • HTTP logic
│  300 lines │      • Business rules
│  of code   │      • Calculations
│            │      • External APIs
│            │      • Complex queries
└────────────┘

WITH Service Layer:
┌────────────┐
│ Controller │ ◄─── CLEAN!
│            │      • HTTP only
│  50 lines  │      • Validate
│  of code   │      • Call service
│            │      • Return response
└─────┬──────┘
      │ delegates
      ▼
┌──────────────┐
│   Service    │ ◄─── FOCUSED!
│              │      • Business logic
│  150 lines   │      • Calculations
│  of code     │      • Complex operations
│              │      • Reusable
└──────────────┘
```

---

## Authentication Flow

```
┌──────────────────────────────────────────────────────┐
│           Token-Based Authentication                 │
└──────────────────────────────────────────────────────┘

Step 1: Login
User → POST /api/login → AuthController
                             │
                             ▼
                        Verify credentials
                             │
                             ▼
                        Create Token (Sanctum)
                             │
                             ▼
                        Return { user, token }

Step 2: Store Token
Frontend receives token
    │
    ▼
Store in:
• Web: localStorage.setItem('token', ...)
• Mobile: AsyncStorage.setItem('token', ...)

Step 3: Use Token
Every API request:
    │
    ▼
Headers: {
    Authorization: "Bearer abc123xyz..."
}

Step 4: Verify Token
Request → Middleware (auth:sanctum)
              │
              ▼
         Verify token
              │
              ├─ Valid → Continue to controller
              │
              └─ Invalid → 401 Unauthorized

Step 5: Logout
POST /api/logout
    │
    ▼
Delete token from database
    │
    ▼
Frontend clears storage
```

---

## API Endpoint Structure

```
┌─────────────────────────────────────────────────────────┐
│                RESTful API Endpoints                    │
└─────────────────────────────────────────────────────────┘

Resource: Teachers

GET    /api/teachers           → List all teachers
POST   /api/teachers           → Create new teacher
GET    /api/teachers/{id}      → Get specific teacher
PUT    /api/teachers/{id}      → Update teacher
DELETE /api/teachers/{id}      → Delete teacher

GET    /api/teachers/{id}/kpis → Get teacher's KPIs
GET    /api/teachers/{id}/evaluations → Get evaluations

Resource: KPIs

GET    /api/kpis               → List all KPIs
POST   /api/kpis               → Create KPI
GET    /api/kpis/{id}          → Get specific KPI
PUT    /api/kpis/{id}          → Update KPI
DELETE /api/kpis/{id}          → Delete KPI

POST   /api/kpis/{id}/progress → Update KPI progress

Resource: CPE

GET    /api/mycpe-records      → List CPE records
POST   /api/mycpe-records      → Add CPE record
DELETE /api/mycpe-records/{id} → Delete record

POST   /api/mycpe-records/{id}/approve → Approve (Principal)
GET    /api/mycpe-records/compliance/check → My compliance

... and 30+ more endpoints
```

---

## Mobile App Architecture

```
┌──────────────────────────────────────────────────────┐
│            React Native Mobile App                   │
└──────────────────────────────────────────────────────┘

┌──────────────┐
│ Entry Point  │
│   App.js     │
└──────┬───────┘
       │
       ▼
┌────────────────────────┐
│   Root Navigator       │
│  (Auth or Main based   │
│   on login status)     │
└──────┬─────────────────┘
       │
       ├─── Is Logged In? ─── YES ──┐
       │                             │
       └─── NO ───┐                  │
                  │                  │
                  ▼                  ▼
         ┌─────────────┐    ┌─────────────────┐
         │    Auth     │    │  Main Navigator │
         │  Navigator  │    │                 │
         ├─────────────┤    ├─────────────────┤
         │ • Login     │    │ Bottom Tabs:    │
         │ • Register  │    │ • Dashboard     │
         └─────────────┘    │ • KPIs          │
                            │ • Evaluations   │
                            │ • CPE           │
                            │ • Profile       │
                            └─────────────────┘
                                    │
                                    ▼
                            ┌────────────────┐
                            │   Services     │
                            │   (REUSED!)    │
                            ├────────────────┤
                            │ • api.js       │
                            │ • authService  │
                            │ • kpiService   │
                            │ • cpeService   │
                            └────────┬───────┘
                                     │
                                     ▼
                              Laravel Backend
                                     │
                                     ▼
                              PostgreSQL DB
```

---

## Data Flow: Adding CPE Record

```
Mobile/Web UI
     │
     │ User fills form:
     │ • Title: "Workshop"
     │ • Hours: 8
     │ • Dates: ...
     │
     ▼
┌─────────────────┐
│ Click "Submit"  │
└────────┬────────┘
         │
         ▼
┌──────────────────────────────┐
│ cpeService.createCPERecord() │
│                              │
│ POST /api/mycpe-records      │
│ Body: {                      │
│   title: "Workshop",         │
│   hours: 8,                  │
│   ...                        │
│ }                            │
└────────┬─────────────────────┘
         │
         │ HTTP + JSON
         ▼
┌──────────────────────────────┐
│ MycpeRecordController@store  │
│                              │
│ 1. Validate input            │
│ 2. Create record             │
│ 3. Send notification         │
│ 4. Return response           │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ MycpeRecord::create([...])   │
│                              │
│ INSERT INTO mycpe_records    │
│ (teacher_id, title, hours,   │
│  status, ...)                │
│ VALUES (...)                 │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ PostgreSQL Database          │
│                              │
│ Row inserted:                │
│ • id: 123                    │
│ • teacher_id: 1              │
│ • title: "Workshop"          │
│ • hours: 8                   │
│ • status: "pending"          │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Response to Frontend:        │
│ {                            │
│   "record": {                │
│     "id": 123,               │
│     "title": "Workshop",     │
│     "status": "pending"      │
│   }                          │
│ }                            │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Frontend Updates:            │
│ • Show success message       │
│ • Add to CPE list            │
│ • Update compliance status   │
└──────────────────────────────┘
```

---

## Why This Architecture Scales for Mobile

```
✅ Stateless API
   • No server-side sessions
   • Easy to scale horizontally
   • Load balancers work perfectly

✅ Token Authentication
   • Works across devices
   • No cookie dependencies
   • Mobile-friendly

✅ RESTful Design
   • Standard HTTP methods
   • Cacheable responses
   • Works with any client

✅ JSON Responses
   • Universal format
   • Easy to parse
   • Compact size

✅ Pagination
   • Saves mobile bandwidth
   • Improves performance
   • Better UX

✅ Service Layer
   • Business logic reusable
   • Easy to test
   • Consistent across clients
```

---

## Summary

**Your backend is perfectly designed for:**
1. ✅ Web applications (current)
2. ✅ Mobile apps (iOS/Android)
3. ✅ Third-party integrations
4. ✅ Future expansion

**Architecture Strengths:**
- ✅ Clear separation of concerns
- ✅ Scalable and maintainable
- ✅ Well-documented
- ✅ Standard conventions
- ✅ Secure by default

**Ready for Mobile?**
- ✅ 95% prepared!
- ✅ Only minor enhancements needed
- ✅ Can start mobile development today!

---

**See also:**
- `BACKEND_ARCHITECTURE_GUIDE.md` - Detailed backend explanation
- `MOBILE_SCALING_GUIDE.md` - How to build mobile app
